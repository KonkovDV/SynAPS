# Known Issues Registry

> SynAPS defect / sentinel registry (audit v4, T-41 / F15; Wave 5 updates;
> evidence close 2026-08-26 N1–N14).
> Every intentional `pytest.mark.xfail` in `tests/test_redteam_guards.py`
> MUST have a row here. Every **Failure taxonomy** slug in current
> `benchmark/BENCHMARK_EVIDENCE_*.md` files MUST appear in a row.
> CI: `tests/test_known_issues_registry.py`.

| ID | Severity | Status | Summary | Sentinel / tests | Tracking |
|---|---|---|---|---|---|
| KI-S3 | MEDIUM | accepted (sentinel) | `compute_machine_tsp_lower_bound` is not monotone under set shrinkage: `L(S) - L(S\\{j})` can be > 0, so a cut that discounts only `p_j` over-claims. Cuts removed; BHK kept for fixed-set LB + docs (T-23). Prefer `compute_min_out_assignment_setup_lb` recomputed on the fixed assigned set (also not absolutely subset-monotone; never discount). **Wave 10: permanent accepted until a sound monotone LB is invented — do not revive cuts.** | `tests/test_redteam_guards.py::test_guard_s3_bhk_bound_subset_monotone` (xfail strict) | F6 / GUARD-S3 / T-23 / Wave 10 |
| KI-F7 | LOW | closed | Exact lane inference holds in-envelope; greedy fallback emits `LANE_INFERENCE_UNPROVEN`. Customer oracle uses `proven_hard_violations` with **WC-scoped** demotion of lane/setup FP kinds only — **not** physical `MACHINE_CAPACITY_VIOLATION` (Wave 8 / Wave 11 / Wave 12 H12-1). | `tests/test_exact_lane_inference.py`, `tests/test_wave11_hyper_rt.py`, `tests/test_wave12_hyper_rt.py` | F7 / Wave 5–12 |
| KI-F16 | LOW | closed (partial pack) | Brandimarte OPT==BKS (F16a); SDST public slice (F16c); M0 `machine_duration_overrides` (Wave 9). Full Shen/dmorill pack: **permanent forbid — dmorill is GPL-3.0** (Wave 10). Native ABI for true `p_{o,m}`: **permanent deferral**; skip-to-Python remains the contract. | `tests/test_public_instances_bks.py`, `tests/test_sdst_fjs_loader.py`, `tests/test_model_field_conformance.py`, `docs/rfc/WAVE10_DEFERRED_DECISIONS.md` | F16 / T-30 / Wave 5–10 |
| KI-N1 | CRITICAL | mitigated | Семейство ALNS неприменимо при ≥5k операций в этой реализации. Measured unconstrained 5000@8 ALNS-500 seeds 1/42/999: 0.0 / `wall_clock_before_search` / 252.927, 252.725, 252.857 (`benchmark/evidence/beam-alns-box-2026-08-26/`). Default `5k@400s` still *routes* to `ALNS-500`; that is the defect, not a usable config at this scale. Hard-window / non-empty calendar instances go to `CALENDAR_AWARE`. Do not lower `global_greedy_cover_min_ops`. Taxonomy: `alns-wall-before-search`, `cover-below-10k`. | `tests/test_solver_portfolio.py::test_route_solver_skips_alns_500_when_hard_windows`, `tests/test_calendar.py::test_route_calendar_instance_returns_calendar_aware_whitelist`, `tests/test_coverage_outcome.py::test_alns_500_zero_scheduled_ops_is_not_feasible` | N1 |
| KI-N2 | CRITICAL | mitigated | `time_limit_s` declared for 25 configs, enforced for GREED family only, BEAM piggyback измерен как косметический (`BeamSearchDispatch._solve_core` does not read the kwarg). Taxonomy `greed-unbounded-stall`: GREED 3000@4 ~14.5 min before the 120 s box. A boxed BEAM-3 3000@4 night cell ran past the declared box. Unboxed matrix deferred past 2026-09-14. | `tests/test_solver_portfolio.py::test_every_named_config_declares_time_limit_s`, `synaps/solvers/greedy_dispatch.py` | N2 |
| KI-N3 | CRITICAL | environment-bound | Remainder hashed cells `5k@4`, `5k@12`, `8k@4/8/12` (and recapture `5k@8`) RHC-GREEDY: epoch JSON is `worker_error` exit 1 at watchdog 210 s, no traceback. **И6.1 new isolate 2026-08-26 seed 1, same Windows machine:** all six cells completed `status=error`, `search_stop_reason=wall_clock`, `MISSING_ASSIGNMENT`, ratios 0.4034 / 0.7648 / 0.8514 / 0.262625 / 0.543875 / 0.721125. No kill, no signal. `worker_peak_rss_raw=null` (Windows has no `resource.ru_maxrss`). Hashed remainder JSON was not rewritten. Linux `ubuntu-latest` recapture (И6.2) is **not run this round**. Taxonomy: `worker-exit-nonzero` on the epoch; recapture is time-box incomplete coverage. | `tests/test_evidence_md_integrity.py::test_hashed_8k4_remainder_stays_worker_error`, `benchmark/evidence/deadzone-5k-2026-08-25/sessions/n3-recapture-2026-08-26/` | N3 |
| KI-N4 | HIGH | open | COVER 100k@200 seed 42 stalled >480 s (killed, 136 leftovers) vs ~13 s siblings. `all_verified_feasible=false`. Taxonomy: `seed-residual-stall`, `residual-fill`, `horizon-clip`, `notary-hard`. Do not retune residual gap-fill. **100k@200 is two of three seeds** in README, README_RU, CHANGELOG Unreleased, and the COVER evidence file. Third-seed recapture is not claimed. | `tests/test_evidence_sha256sums.py`, `benchmark/BENCHMARK_EVIDENCE_COVER_2026_08_26.md` | N4 |
| KI-N5 | HIGH | documented | Two published number sets for kernel `bd09d13`: CHANGELOG seed=1 500k (499770 ops / ~145 s / ~2.3 GiB) vs hashed ladder (500000 ops / 71–74 s / ~3.96 GiB). 100k ~26 s vs 12.6–13.7 s. Ladder RSS probe failed on early cells. Citable source is `cover-ladder-2026-08-25/`. Do not mix the rows. | `CHANGELOG.md` Unreleased, `benchmark/BENCHMARK_EVIDENCE_COVER_2026_08_26.md` | N5 |
| KI-N6 | HIGH | mitigated | This registry had only KI-S3 / KI-F7 / KI-F16 on the day the 2026-08-26 evidence files shipped. N1–N12 are now rows. | `tests/test_known_issues_registry.py`, `tests/test_claims_lint.py` | N6 |
| KI-N7 | HIGH | closed | Shift calendar primitive + occupancy notary. Night analog remains per-op windows (`night-window-coverage-gap`). GREED/BEAM/RHC-GREEDY clip **setup and processing** via `find_earliest_feasible_slot`; native COVER skips. CP-SAT/ALNS/LBBD **refuse** a non-empty calendar (empty `ERROR`). Checker validates `[start − setup, end]`. ADR-0005. | `tests/test_calendar.py::test_checker_rejects_setup_occupying_closed_shift`, `docs/adr/0005-work-center-calendar.md` | N7 |
| KI-N8 | MEDIUM | accepted (orphaned) | P2.3 `environment.json` was overwritten by the remainder `--resume` session (file itself says orphaned). Bytes cannot be restored. Freeze copies of `run_*.json` and `SHA256SUMS_p2_3.txt` remain. Do not rewrite hashed JSON. | `benchmark/evidence/deadzone-5k-2026-08-25/environment.json` | N8 |
| KI-N9 | MEDIUM | mitigated | Cable n=10 t-interval was hand-computed because `summarize_seed` only emitted CI for n=3. Helper now covers n=2..31. Hashed cable JSON is unchanged. | `tests/test_evidence_common.py`, `benchmark/evidence_common.py` | N9 |
| KI-N10 | MEDIUM | open | Windows-only hashed ladder. RSS probe failed on 60k and 100k seed 1 (`rss-probe-failed`). Linux COVER not in PR CI (no native wheel). `cpu_flags` captured on Linux only. | `benchmark/evidence_common.py::cpu_flags` | N10 |
| KI-N11 | MEDIUM | partial | Honesty lists lived only inside evidence markdown. CI scans README / CHANGELOG / KNOWN_ISSUES with a denylist, an explicit non-claims marker (not a `не`/`not` skip), Russian FORBIDDEN/PERF_NUM parity, and performance numbers that lack an evidence path on the same or neighboring line. Not a full claims parser. | `tests/test_claims_lint.py`, `scripts/verify_claims.py` | N11 |
| KI-N12 | MEDIUM | open | Domain pins lag kernel `515488b`: GridPlan still on `bd09d13`, MobiRoute on `5168fc7`. Hygiene closed on the kernel only. Do not bump pins in this change (ADR 0004). Sync date **2026-09-09**: domains must then pass fail-closed coverage, calendar refuse, and claims-lint. Open-ended “pin lag allowed” after that date is forbidden. | `docs/adr/0004-domain-repo-standard.md` | N12 |
| KI-N13 | MEDIUM | closed | COVER evidence narrative once said the sums table was stale while the Artifact SHA-256 row still listed the correct LF digest `e5caad9a`. Retracted. Digest is the git blob; `benchmark/evidence/**` is `-text`. On master `9b506342` (PR #11, Linux run [33007702599](https://github.com/KonkovDV/SynAPS/actions/runs/33007702599)). | `tests/test_evidence_md_integrity.py::test_artifact_sha256_table_matches_sha256sums_txt`, `tests/test_evidence_md_integrity.py::test_artifact_sha256_table_fails_on_planted_mismatch` | N13 |
| KI-N14 | HIGH | open | `test-slow` is red on master. Node: `tests/test_benchmark_solver_scaling_study.py::test_study_solver_scaling_compares_requested_solvers_for_large_preset` (`assert False is True`). Last fully-green workflow (including test-slow): `bd09d13` run [31974734700](https://github.com/KonkovDV/SynAPS/actions/runs/31974734700) — also the GridPlan kernel pin. Fail run on `515488b`: [32987597757](https://github.com/KonkovDV/SynAPS/actions/runs/32987597757). Verdict (harness vs silent skip vs stale test) is K2.2; due **2026-09-09** so a domain pin bump is not a quality drop vs `bd09d13`. | `tests/test_benchmark_solver_scaling_study.py::test_study_solver_scaling_compares_requested_solvers_for_large_preset` | N14 |

## How to add an entry

1. Prefer a **repro test that fails** (`xfail` only for intentional sentinels).
2. Add a row with a stable `KI-*` id, severity, status, and the exact pytest node id.
3. Reference the finding id from `docs/audit/REDTEAM_AUDIT_V4_ALGEBRA_PLAN_2026_08_11.md` when applicable.
4. When the defect is fixed: flip status to `closed`, remove the `xfail`, and keep the row for history.

## Deferred past 2026-09-14

Conscious deferral, **not forgotten**:

- И5.1 BEAM-3/BEAM-5 **unboxed** (`time_limit_s` omitted) comparison, if boxed cells
  do not finish in this round.
- И6.2: one 8k@4 seed 1 cell on `ubuntu-latest` without native.

Do not retune `global_greedy_cover_min_ops` to chase a Yes on 5k.

