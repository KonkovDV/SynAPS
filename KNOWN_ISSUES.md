# Known Issues Registry

> SynAPS defect / sentinel registry (audit v4, T-41 / F15; Wave 5 updates;
> evidence close 2026-08-26 N1–N14).
> Every intentional `pytest.mark.xfail` in `tests/test_redteam_guards.py`
> MUST have a row here. Every **Failure taxonomy** slug in current
> `benchmark/BENCHMARK_EVIDENCE_*.md` files MUST appear in a row.
> CI: `tests/test_known_issues_registry.py`.

| ID | Severity | Status | Summary | Sentinel / tests | Tracking |
|---|---|---|---|---|---|
| KI-S3 | MEDIUM | accepted (sentinel) | `compute_machine_tsp_lower_bound` is not monotone under set shrinkage: `L(S) - L(S\\{j})` can be > 0, so a cut that discounts only `p_j` over-claims. Cuts removed; BHK kept for fixed-set LB + docs (T-23). Prefer `compute_min_out_assignment_setup_lb` recomputed on the fixed assigned set (also not absolutely subset-monotone; never discount). **Wave 10: permanent accepted until a sound monotone LB is invented — do not revive cuts.** | `tests/test_redteam_guards.py::test_guard_s3_bhk_bound_subset_monotone` (xfail strict) | F6 / GUARD-S3 / T-23 / Wave 10 |
| KI-F7 | LOW | closed | Exact lane inference holds in-envelope; greedy fallback emits `LANE_INFERENCE_UNPROVEN`. Customer oracle uses `proven_hard_violations` with **WC-scoped** demotion of lane/setup FP kinds only — **not** physical `MACHINE_CAPACITY_VIOLATION` (Wave 8 / Wave 11 / Wave 12 H12-1). | `tests/test_exact_lane_inference.py`, `tests/test_wave11_hyper_rt.py`, `tests/test_wave12_hyper_rt.py` | F7 / Wave 5–12 |
| KI-F16 | LOW | closed (partial pack) | Brandimarte OPT==BKS (F16a); SDST public slice (F16c); M0 `machine_duration_overrides` (Wave 9). Full Shen/dmorill pack: **permanent forbid — dmorill is GPL-3.0** (Wave 10). Native ABI for true `p_{o,m}`: **permanent deferral**; skip-to-Python remains the contract. | `tests/test_public_instances_bks.py`, `tests/test_sdst_fjs_loader.py`, `tests/test_model_field_conformance.py`, `docs/rfc/WAVE10_DEFERRED_DECISIONS.md` | F16 / T-30 / Wave 5–10 |
| KI-N1 | CRITICAL | mitigated | ALNS is not a complete ≥5k solver **on hashed 5k@8**. Epoch unconstrained 5000@8 ALNS-500 seeds 1/42/999: 0.0 / `wall_clock_before_search` (`benchmark/evidence/beam-alns-box-2026-08-26/`). **List-schedule seed session** (unconstrained): ratio 1.0 (`benchmark/BENCHMARK_EVIDENCE_ALNS_500_5K_LIST_SCHEDULE_2026_08_27.md`). Windowed night leftover fill now uses a clipped interior scan; COVER list-schedule at n>=2000 when hard windows exist (10_000 unconstrained gate unchanged). Hashed P2.3 freeze stays **no**. Do not lower `global_greedy_cover_min_ops`. Taxonomy: `alns-wall-before-search`, `cover-below-10k`, `night-window-coverage-gap`. | `tests/test_night_window_scan.py::test_greed_closes_tiny_night_stamped_instance`, `tests/test_alns_append_seed.py::test_alns_list_schedule_seed_covers_unconstrained_at_append_threshold` | N1 |
| KI-N2 | CRITICAL | mitigated | `time_limit_s` declared for 25 configs. GREED family and BEAM now share a deadline: `BeamSearchDispatch.solve` splits remaining budget across widths 1..B and `_solve_core` reads `time_limit_s`. Sentinel: `tests/test_timebox_enforcement.py::test_beam_stops_when_slot_search_is_slow`. Unboxed 12-cell BEAM matrix deferred past 2026-09-14. **PR CI** `native-accelerator` runs those two BEAM timebox nodes on Linux. Linux run id: **не проверено на Linux** until this PR's job. Taxonomy: `greed-unbounded-stall`. | `tests/test_timebox_enforcement.py::test_beam_stops_when_slot_search_is_slow`, `.github/workflows/ci.yml` `BEAM timebox on Linux` | N2 |
| KI-N3 | CRITICAL | algorithmic | Remainder hashed cells `5k@4`, `5k@12`, `8k@4/8/12` (and recapture `5k@8`) RHC-GREEDY: epoch JSON stays `worker_error` (not rewritten). **K2.3 Linux `ubuntu-latest` native wheel, seed 1, six cells** run [33021109132](https://github.com/KonkovDV/SynAPS/actions/runs/33021109132): all completed `status=error`, `search_stop_reason=wall_clock`, not `worker_error`. Ratios 0.4012 / 0.7666 / 0.8514 / 0.261125 / 0.549125 / 0.70975. Notary `MISSING_ASSIGNMENT`; 8k@12 also `MACHINE_OVERLAP`. Matches the Windows isolate (И6.1) class: time-box incomplete coverage, not an OS kill. Taxonomy: `worker-exit-nonzero` on the hashed epoch; recapture is algorithmic. | `tests/test_evidence_md_integrity.py::test_hashed_8k4_remainder_stays_worker_error`, `benchmark/evidence/deadzone-5k-2026-08-25/sessions/n3-linux-2026-08-27/` | N3 |
| KI-N4 | HIGH | mitigated | Hashed COVER 100k@200 seed 42 stays STALL >480 s (`run_100k_at_200_seed42.json`). **Session recapture** after residual append-scan: 100000/100000, `verified_feasible=true`, `native_backend=python`, wall 40.137 s (`benchmark/BENCHMARK_EVIDENCE_COVER_100K_SEED42_2026_08_27.md`). Do not mix with hashed native ~13 s siblings. Do not retune residual placement to chase a hashed three-seed Yes. Taxonomy: `seed-residual-stall`, `residual-fill`, `horizon-clip`, `notary-hard`. Hashed **100k@200 is two of three seeds**. | `tests/test_evidence_md_integrity.py::test_cover_100k_seed42_session_is_full_python_cover`, `tests/test_rhc_cover.py::test_residual_greedy_uses_append_scan_when_timeline_is_large` | N4 |
| KI-N5 | HIGH | documented | Two published number sets for kernel `bd09d13`. **Cite only** hashed `cover-ladder-2026-08-25/` (500000 ops / 71–74 s / ~3.96 GiB). The Unreleased dump 499770 / ~145 s / ~2.3 GiB is non-citable (`<!-- non-claims -->`). Do not mix the rows. | `CHANGELOG.md` Unreleased, `benchmark/BENCHMARK_EVIDENCE_COVER_2026_08_26.md` | N5 |
| KI-N6 | HIGH | mitigated | This registry had only KI-S3 / KI-F7 / KI-F16 on the day the 2026-08-26 evidence files shipped. N1–N12 are now rows. | `tests/test_known_issues_registry.py`, `tests/test_claims_lint.py` | N6 |
| KI-N7 | HIGH | closed | Shift calendar primitive + occupancy notary. Night analog remains per-op windows (`night-window-coverage-gap`). GREED/BEAM/RHC-GREEDY clip **setup and processing** via `find_earliest_feasible_slot`; native COVER skips. CP-SAT/ALNS/LBBD **refuse** a non-empty calendar (empty `ERROR`). Checker validates `[start − setup, end]`. ADR-0005. | `tests/test_calendar.py::test_checker_rejects_setup_occupying_closed_shift`, `docs/adr/0005-work-center-calendar.md` | N7 |
| KI-N8 | MEDIUM | accepted (orphaned) | P2.3 `environment.json` was overwritten by the remainder `--resume` session (file itself says orphaned). Bytes cannot be restored. Freeze copies of `run_*.json` and `SHA256SUMS_p2_3.txt` remain. Do not rewrite hashed JSON. | `benchmark/evidence/deadzone-5k-2026-08-25/environment.json` | N8 |
| KI-N9 | MEDIUM | mitigated | Cable n=10 t-interval was hand-computed because `summarize_seed` only emitted CI for n=3. Helper now covers n=2..31. Hashed cable JSON is unchanged. | `tests/test_evidence_common.py`, `benchmark/evidence_common.py` | N9 |
| KI-N10 | MEDIUM | mitigated | Windows-only hashed ladder. RSS probe failed on 60k and 100k seed 1 (`rss-probe-failed`). `cpu_flags` Linux-only. **PR CI** `native-accelerator` runs `study_cover_ladder --scales 60k@100 --seeds 1 --ci-gate` on the native wheel. Linux run [33119219605](https://github.com/KonkovDV/SynAPS/actions/runs/33119219605): 60k@100 seed 1 ratio 1.0, native, wall 8.919 s (`docs/rfc/REDTEAM_TRIAGE_RESIDUALS_2026_08_27.md`). Not a re-run of hashed 500k. | `benchmark/study_cover_ladder.py::_ci_gate_failures`, `.github/workflows/ci.yml` `COVER ladder cell on Linux` | N10 |
| KI-N11 | MEDIUM | partial | Honesty lists lived only inside evidence markdown. CI scans README / CHANGELOG / KNOWN_ISSUES with a denylist, an explicit non-claims marker (not a `не`/`not` skip), Russian FORBIDDEN/PERF_NUM parity, performance numbers that lack an evidence path on the same or neighboring line, and README_RU sourced PERF_NUM ratio ≥90% (`scripts/verify_claims.py --stats`). Not a full claims parser. | `tests/test_claims_lint.py::test_readme_ru_sourced_perf_num_ratio_at_least_90_percent`, `scripts/verify_claims.py` | N11 |
| KI-N12 | MEDIUM | open | Pin PRs opened on `54ebf9f`: [GridPlan #7](https://github.com/KonkovDV/SynAPS-GridPlan/pull/7), [MobiRoute #4](https://github.com/KonkovDV/SynAPS-MobiRoute/pull/4). Stays open until those merge. ADR-0004 lag-until-2026-09-09 is closed as a *permission to wait*. Open-ended lag after 2026-09-09 remains forbidden. | `docs/adr/0004-domain-repo-standard.md` | N12 |
| KI-N13 | MEDIUM | closed | COVER evidence narrative once said the sums table was stale while the Artifact SHA-256 row still listed the correct LF digest `e5caad9a`. Retracted. Digest is the git blob; `benchmark/evidence/**` is `-text`. On master `9b506342` (PR #11, Linux run [33007702599](https://github.com/KonkovDV/SynAPS/actions/runs/33007702599)). | `tests/test_evidence_md_integrity.py::test_artifact_sha256_table_matches_sha256sums_txt`, `tests/test_evidence_md_integrity.py::test_artifact_sha256_table_fails_on_planted_mismatch` | N13 |
| KI-N14 | HIGH | closed | `test-slow` on default `main` is green. Node: `tests/test_benchmark_solver_scaling_study.py::test_study_solver_scaling_compares_requested_solvers_for_large_preset`. **K2.2 verdict: (a), not (b).** Run [32987597757](https://github.com/KonkovDV/SynAPS/actions/runs/32987597757): LBBD-10 ran (`routed: true`, `op_count: 177`, solver `status: feasible`, `assignment_count: 177`, `duration_ms: 60018`). `results.feasible` is FULL coverage AND status in {feasible,optimal} AND independent notary (`benchmark/run_benchmark.py`). The extra `feasible is True` assert is removed; the test now requires that LBBD-10 ran and `feasible` is a bool. Preset `large` is ~177 ops, not the COVER 50k–500k ladder — **no retraction** of hashed COVER numbers. Windows 2026-08-27 on `k3/honesty-close`: the node passed (128.79 s). Linux `main` push [33103963622](https://github.com/KonkovDV/SynAPS/actions/runs/33103963622): `test-slow` success (21m24s) with the rest of required CI. | `tests/test_benchmark_solver_scaling_study.py::test_study_solver_scaling_compares_requested_solvers_for_large_preset` | N14 |
| KI-N15 | HIGH | closed | Independent `verify_schedule_result` skipped the checker whenever status was not FEASIBLE/OPTIMAL, so studies published empty `independent_violation_kinds` next to a solver notary on ERROR/TIMEOUT with partial assignments. Checker now runs when assignments are nonempty; `feasible` stays True only for FEASIBLE/OPTIMAL with empty `proven_hard_violations`. Empty-assignment failures still skip (500k MISSING_ASSIGNMENT). | `tests/test_calendar.py::test_verify_error_with_assignments_still_runs_notary`, `tests/test_calendar.py::test_verify_error_with_clean_assignments_is_not_verified_feasible` | N15 |

## How to add an entry

1. Prefer a **repro test that fails** (`xfail` only for intentional sentinels).
2. Add a row with a stable `KI-*` id, severity, status, and the exact pytest node id.
3. Reference the finding id from `docs/audit/REDTEAM_AUDIT_V4_ALGEBRA_PLAN_2026_08_11.md` when applicable.
4. When the defect is fixed: flip status to `closed`, remove the `xfail`, and keep the row for history.

## Deferred past 2026-09-14

Conscious deferral, **not forgotten**:

- И5.1 BEAM-3/BEAM-5 **unboxed** (`time_limit_s` omitted) comparison, if boxed cells
  do not finish in this round.
- И6.2: one 8k@4 seed 1 cell on `ubuntu-latest` without native.

Do not retune `global_greedy_cover_min_ops` to chase a Yes on 5k.

