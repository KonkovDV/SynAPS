# Known Issues Registry

> SynAPS defect / sentinel registry (audit v4, T-41 / F15; Wave 5 updates;
> evidence close 2026-08-26 N1–N12).
> Every intentional `pytest.mark.xfail` in `tests/test_redteam_guards.py`
> MUST have a row here. Every **Failure taxonomy** slug in current
> `benchmark/BENCHMARK_EVIDENCE_*.md` files MUST appear in a row.
> CI: `tests/test_known_issues_registry.py`.

| ID | Severity | Status | Summary | Sentinel / tests | Tracking |
|---|---|---|---|---|---|
| KI-S3 | MEDIUM | accepted (sentinel) | `compute_machine_tsp_lower_bound` is not monotone under set shrinkage: `L(S) - L(S\\{j})` can be > 0, so a cut that discounts only `p_j` over-claims. Cuts removed; BHK kept for fixed-set LB + docs (T-23). Prefer `compute_min_out_assignment_setup_lb` recomputed on the fixed assigned set (also not absolutely subset-monotone; never discount). **Wave 10: permanent accepted until a sound monotone LB is invented — do not revive cuts.** | `tests/test_redteam_guards.py::test_guard_s3_bhk_bound_subset_monotone` (xfail strict) | F6 / GUARD-S3 / T-23 / Wave 10 |
| KI-F7 | LOW | closed | Exact lane inference holds in-envelope; greedy fallback emits `LANE_INFERENCE_UNPROVEN`. Customer oracle uses `proven_hard_violations` with **WC-scoped** demotion of lane/setup FP kinds only — **not** physical `MACHINE_CAPACITY_VIOLATION` (Wave 8 / Wave 11 / Wave 12 H12-1). | `tests/test_exact_lane_inference.py`, `tests/test_wave11_hyper_rt.py`, `tests/test_wave12_hyper_rt.py` | F7 / Wave 5–12 |
| KI-F16 | LOW | closed (partial pack) | Brandimarte OPT==BKS (F16a); SDST public slice (F16c); M0 `machine_duration_overrides` (Wave 9). Full Shen/dmorill pack: **permanent forbid — dmorill is GPL-3.0** (Wave 10). Native ABI for true `p_{o,m}`: **permanent deferral**; skip-to-Python remains the contract. | `tests/test_public_instances_bks.py`, `tests/test_sdst_fjs_loader.py`, `tests/test_model_field_conformance.py`, `docs/rfc/WAVE10_DEFERRED_DECISIONS.md` | F16 / T-30 / Wave 5–10 |
| KI-N1 | CRITICAL | mitigated | Default `5k@400s` still routes unconstrained instances to `ALNS-500`. On the 5k night-window analog that config scheduled **0 ops** on seeds 1/42/999 (`wall_clock_before_search`). Router now sends hard-window **or calendar-only** instances to `RHC-GREEDY` when a latency hint would otherwise pick ALNS (`has_per_op_windows or has_machine_calendar`). Without a latency hint, 5k+calendar still selects `LBBD-10-HD`, which refuses the calendar. Do not lower `global_greedy_cover_min_ops` to chase a Yes. Unconstrained 5k@400s remains ALNS-500; whether that path enters search on this tree is unmeasured this round. Taxonomy: `alns-wall-before-search`, `cover-below-10k`. | `tests/test_solver_portfolio.py::test_route_solver_skips_alns_500_when_hard_windows`, `tests/test_calendar.py::test_route_never_selects_alns_on_calendar_without_windows`, `tests/test_solver_portfolio.py::test_every_named_config_declares_time_limit_s` | N1 |
| KI-N2 | CRITICAL | mitigated | `GREED` / `GREED-K1-3` now have `time_limit_s=120` because the measured stall was GREED on 3000@4 (~14.5 min, taxonomy `greed-unbounded-stall`). `BEAM-3` / `BEAM-5` received the same box without a measured BEAM stall; that piggyback is unmeasured this round and is not a Yes. | `tests/test_solver_portfolio.py::test_every_named_config_declares_time_limit_s`, `tests/test_solver_portfolio.py::test_time_limit_gate_fails_on_planted_missing_box` | N2 |
| KI-N3 | CRITICAL | open | Remainder dead-zone cells `5k@4`, `5k@12`, `8k@4/8/12` (RHC-GREEDY, all three seeds): hashed JSON is `worker_error` exit 1 at watchdog 210 s, no traceback (`worker-exit-nonzero`). Recapture 8k@4 seed 1 on tree `7a4083e` did **not** reproduce the crash: CPython 3.13.7 + native, wall 145.212 s, ratio 0.264375, `MISSING_ASSIGNMENT`, `status=error` (time box). Isolate now writes a sidecar `.json.stderr.txt`, returncode/signal, and worker peak RSS on **new** sessions. Hashed remainder JSON was not rewritten. Six original cells were not re-run this round; Linux recapture is absent. Epoch kill remains unexplained. | `tests/test_evidence_md_integrity.py::test_hashed_8k4_remainder_stays_worker_error`, `benchmark/evidence/deadzone-5k-2026-08-25/sessions/worker-error-2026-08-26-py313/` | N3 |
| KI-N4 | HIGH | open | COVER 100k@200 seed 42 stalled >480 s (killed, 136 leftovers) vs ~13 s siblings. `all_verified_feasible=false`. Taxonomy: `seed-residual-stall`, `residual-fill`, `horizon-clip`, `notary-hard`. Do not retune residual gap-fill. README must not say 100k passes on three seeds. | `tests/test_evidence_sha256sums.py`, `benchmark/BENCHMARK_EVIDENCE_COVER_2026_08_26.md` | N4 |
| KI-N5 | HIGH | documented | Two published number sets for kernel `bd09d13`: CHANGELOG seed=1 500k (499770 ops / ~145 s / ~2.3 GiB) vs hashed ladder (500000 ops / 71–74 s / ~3.96 GiB). 100k ~26 s vs 12.6–13.7 s. Ladder RSS probe failed on early cells. Citable source is `cover-ladder-2026-08-25/`. Do not mix the rows. | `CHANGELOG.md` Unreleased, `benchmark/BENCHMARK_EVIDENCE_COVER_2026_08_26.md` | N5 |
| KI-N6 | HIGH | mitigated | This registry had only KI-S3 / KI-F7 / KI-F16 on the day the 2026-08-26 evidence files shipped. N1–N12 are now rows. | `tests/test_known_issues_registry.py`, `tests/test_claims_lint.py` | N6 |
| KI-N7 | HIGH | partial | Shift calendar primitive + `CALENDAR_VIOLATION`. Night analog remains per-op windows (`night-window-coverage-gap`). GREED/BEAM/RHC-GREEDY clip **processing** via `find_earliest_feasible_slot`; native COVER skips. CP-SAT/ALNS/LBBD **refuse** a non-empty calendar (empty `ERROR`, not silent FEASIBLE). Shifts are not encoded in CP-SAT. SDST setup before `start` is not clipped (can occupy a closed shift). ADR-0005. | `tests/test_calendar.py`, `docs/adr/0005-work-center-calendar.md` | N7 |
| KI-N8 | MEDIUM | accepted (orphaned) | P2.3 `environment.json` was overwritten by the remainder `--resume` session (file itself says orphaned). Bytes cannot be restored. Freeze copies of `run_*.json` and `SHA256SUMS_p2_3.txt` remain. Do not rewrite hashed JSON. | `benchmark/evidence/deadzone-5k-2026-08-25/environment.json` | N8 |
| KI-N9 | MEDIUM | mitigated | Cable n=10 t-interval was hand-computed because `summarize_seed` only emitted CI for n=3. Helper now covers n=2..31. Hashed cable JSON is unchanged. | `tests/test_evidence_common.py`, `benchmark/evidence_common.py` | N9 |
| KI-N10 | MEDIUM | open | Windows-only hashed ladder. RSS probe failed on 60k and 100k seed 1 (`rss-probe-failed`). Linux COVER not in PR CI (no native wheel). `cpu_flags` captured on Linux only. | `benchmark/evidence_common.py::cpu_flags` | N10 |
| KI-N11 | MEDIUM | partial | Honesty lists lived only inside evidence markdown. CI scans README / CHANGELOG / KNOWN_ISSUES with a denylist, an explicit non-claims marker (not a `не`/`not` skip), and performance numbers that lack an evidence path on the same or neighboring line. Not a full claims parser. | `tests/test_claims_lint.py`, `scripts/verify_claims.py` | N11 |
| KI-N12 | MEDIUM | open | Domain pins lag kernel `7a4083e`: GridPlan still on `bd09d13`, MobiRoute on `5168fc7`. Hygiene closed on the kernel only. Do not bump pins in this change (ADR 0004). Sync date **2026-09-09**: domains must then pass fail-closed coverage, calendar refuse, and claims-lint. Open PRs as of 2026-08-26: GridPlan #5 (docs, rust check red), #4/#2/#1 Dependabot; MobiRoute #1 (docs, typecheck red). Not merged while checks fail. | `docs/adr/0004-domain-repo-standard.md` | N12 |

## How to add an entry

1. Prefer a **repro test that fails** (`xfail` only for intentional sentinels).
2. Add a row with a stable `KI-*` id, severity, status, and the exact pytest node id.
3. Reference the finding id from `docs/audit/REDTEAM_AUDIT_V4_ALGEBRA_PLAN_2026_08_11.md` when applicable.
4. When the defect is fixed: flip status to `closed`, remove the `xfail`, and keep the row for history.
