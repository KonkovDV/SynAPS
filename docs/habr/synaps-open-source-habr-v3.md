# Планирование без чёрного ящика: что реально умеет SynAPS и почему 50K пока не взлетел

31 марта 2026 года я попал на открытую лекцию MOSITLAB про ИИ на производстве. На слайдах был очень сильный кейс для Москабельмета: десятки тысяч операций, сотни рабочих центров, огромная матрица переналадок, заметная экономия. Меня зацепили не цифры сами по себе, а архитектура. В центре стояла нейросеть.

У меня на таких местах всегда один и тот же вопрос: хорошо, а почему система приняла именно это решение?

Если ответ сводится к "модель так решила", мне некомфортно. В производственном планировании это плохая привычка.

Поэтому я делаю [SynAPS](https://github.com/KonkovDV/SynAPS) как противоположный полюс: детерминированный движок планирования, в котором можно открыть код, посмотреть конфигурацию решателя, прогнать независимую проверку допустимости и не спорить о магии.

Сейчас в репозитории уже есть отдельный 50K benchmark-path. И он очень полезен именно потому, что не рисует красивую победу там, где её пока нет.

<habracut />

## Что это вообще за класс задач

Формальная вывеска длинная: MO-FJSP-SDST-ARC. По-русски смысл такой: нужно разложить операции по рабочим центрам, учесть порядок внутри заказа, время переналадки между состояниями, вспомогательные ресурсы вроде оснастки или экипажа, дедлайны и сразу несколько конфликтующих целей.

На практике это означает неприятную вещь. Нельзя просто минимизировать один `makespan` и радоваться. В реальном цехе у вас одновременно дерутся простои, переналадки, потери материала, просрочки и стабильность уже собранного плана.

И да, это NP-трудная история. Если пытаться закрыть всё одним точным солвером, довольно быстро упираешься в стену. Если уйти в одну жадную эвристику, время ответа становится красивым, а качество ответа - не очень.

## Что уже есть в SynAPS

Сейчас SynAPS - не один решатель, а портфель.

| Слой | Что внутри | Для чего нужен |
|---|---|---|
| Точный | `CP-SAT`, `Pareto Slice` | малые и средние инстансы, где важны нижние границы и `gap` |
| Декомпозиционный | `LBBD`, `LBBD-HD` | более крупные задачи, где монолитный `CP-SAT` уже тяжёл |
| Конструктивный | `GREED`, `BEAM` | быстрый допустимый ответ с детерминированным поведением |
| Крупномасштабный | `ALNS`, `RHC` | синтетические большие инстансы и поиск узких мест |
| Контроль качества | `FeasibilityChecker` | независимая проверка результата после solve-path |

Публичный реестр сейчас даёт 21 именованную конфигурацию. Авторитетный список лежит в [synaps/solvers/registry.py](../../synaps/solvers/registry.py).

Это важнее, чем кажется. Тут нет истории вида "у нас есть большая умная модель". Тут всегда можно сказать, какой именно путь сработал: `CPSAT-30`, `LBBD-10-HD`, `ALNS-500`, `RHC-ALNS` и так далее.

## Что поменялось в апреле 2026

Апрель был не про косметику.

Во-первых, `ALNS` и `RHC` окончательно стали публичными поверхностями, а не черновиками в глубине кода. Во-вторых, появился отдельный воспроизводимый study для 50K-сценария: [benchmark/study_rhc_50k.py](../../benchmark/study_rhc_50k.py).

Во-третьих, `RHC` начал отдавать полезную служебную метрику: время предобработки, размер кандидатного пула на окно, продвижение фронтира, признак исчерпания глобального time budget.

И ещё одна вещь, которая снаружи почти незаметна, но внутри важна: после нового прогона выяснилось, что `RHC` тратит слишком много времени не только на саму работу по окнам, но и на попытку "дочистить" хвост после исчерпания времени. Этот путь я исправил. Теперь long-running study не теряется, а честно пишет артефакт с частичным результатом и явной причиной остановки.

Это лучше, чем делать вид, что всё хорошо.

## Что показывает отдельный 50K прогон

Команда сейчас выглядит так:

```bash
python -m benchmark.study_rhc_50k \
    --preset industrial-50k \
    --seeds 1 \
    --solvers RHC-GREEDY RHC-ALNS \
    --write-dir benchmark/studies/2026-04-12-rhc-50k
```

Результат сохраняется в [benchmark/studies/2026-04-12-rhc-50k/rhc_50k_study.json](../../benchmark/studies/2026-04-12-rhc-50k/rhc_50k_study.json).

Смысл этого файла очень простой: он фиксирует не презентационную версию реальности, а настоящую.

```json
{
    "solver_config": "RHC-ALNS",
    "results": {
        "status": "error",
        "feasible": false,
        "assignments": 944
    },
    "statistics": {
        "wall_time_s_mean": 300.184
    },
    "solver_metadata": {
        "peak_window_candidate_count": 49993,
        "time_limit_reached": true,
        "fallback_repair_skipped": true
    }
}
```

По текущему артефакту картина такая:

- `RHC-GREEDY` укладывается в `120.087` секунды, но фиксирует только `887` операций из `50000`
- `RHC-ALNS` тратит `300.184` секунды и фиксирует `944` операции из `50000`
- оба прогона завершаются с `status=error`
- оба прогона не дают допустимого полного расписания
- оба упираются в первое окно

Это не тот абзац, который приятно писать в маркетинговом тексте. Но это именно тот абзац, который нужен в инженерном тексте.

## Где сейчас настоящее узкое место

После артефакта стало видно, что проблема сейчас не в `FeasibilityChecker` и не в том, что study-файлы раньше не писались на диск.

Проблема в другом: первое окно `RHC` притягивает почти весь инстанс сразу. Пиковый кандидатный пул доходит до `49 931` и `49 993` операций. То есть временная декомпозиция формально есть, а фактически первое окно ведёт себя как почти весь набор задач.

Это хороший пример того, почему я люблю белый ящик. Если бы здесь стояла модель с закрытым inference path, пришлось бы долго гадать, где именно всё разваливается. А тут видно: admission logic окна почти не режет поток кандидатов, и дальше никакая локальная оптимизация не спасает.

Глобальный fallback repair после исчерпания времени я уже убрал из критического пути. Значит следующий шаг тоже понятен.

Нужно чинить не запись артефакта и не валидацию, а сам механизм admission в `RHC`:

1. жёстче резать окно до запуска inner solver
2. не тащить в первое окно почти всю earliest-frontier массу
3. отдельно усиливать due-pressure path, который в текущем 50K артефакте остаётся нулевым

Только после этого имеет смысл снова думать о следующем native seam или о более глубокой микрооптимизации repair path.

## Что уже можно утверждать честно

Вот набор утверждений, которые я сейчас готов защищать кодом и артефактами.

| Утверждение | Статус |
|---|---|
| В репозитории есть детерминированный solver-портфель | да |
| Публичный реестр даёт 21 конфигурацию | да |
| Есть независимая проверка допустимости результата | да |
| Есть отдельный воспроизводимый 50K benchmark-path | да |
| `RHC` и `ALNS` уже полностью заведены в публичный runtime и benchmark surface | да |
| Текущий `industrial-50k` preset уже решается до полного допустимого расписания в публичном бюджете времени | нет |
| Есть подтверждение на живом заводе | нет |

Именно так я бы это и формулировал в README, в докладе и в публичной статье. Без красивых эвфемизмов.

## Зачем вообще публиковать проект в таком состоянии

Потому что закрытый чёрный ящик умеет ошибаться так же, как открытый. Просто в закрытом случае вы узнаете об этом позже и дороже.

Если open-source движок пишет артефакт и показывает: вот тут я упёрся в первое окно, вот тут кандидатов слишком много, вот тут закончился бюджет времени, с этим уже можно работать. Это инженерная поверхность.

Меня такой результат устраивает больше, чем красивый скриншот с фразой "масштабируется до 50K+", под которой ничего нельзя проверить.

## Что дальше

Следующая практическая задача для SynAPS не звучит героически. Нужно научить `RHC` не затягивать почти весь `industrial-50k` инстанс в первое окно.

Если это получится, следующий 50K артефакт будет уже не про остановку по времени, а про реальное качество расписания. Если не получится, это тоже будет видно сразу.

В этом и смысл проекта.
|-----------|---------------|-----|
| **Кабельное (Москабельмет)** | Смена оснастки экструдера: 15–240 мин | Мостовой кран, ОТК |
| **Непрерывное литьё стали** | Высокоугл. → низкоугл.: 4 ч; обратно: 30 мин. Тонны лома | МНЛЗ, прокатка |
| **Фармацевтика (GMP)** | Валидация между сериями: 8–12 ч | Чистые комнаты |
| **Автосборка (SMED)** | Смена цвета: пурпурный→белый = 2 ч; белый→белый = 15 мин | Окрасочные камеры |
| **Пищевое производство** | Молоко→кефир = 45 мин; кефир→молоко = 90 мин + стерилизация | Аллерген-контроль |
| **PCB assembly** | Смена фидеров: секунды–минуты. 30K типов компонентов | AOI-инспекция |

## Ghost Setup: баг, который прошёл 100% тестов

Ранняя версия моделировала переналадку как «мёртвое время» — зазор на таймлайне станка. Потом я добавил вспомогательные ресурсы. И обнаружил: во время четырёхчасовой замены футеровки мостовой кран, оператор и стенд *заняты* — но планировщик считает их свободными. Переналадка жрёт ресурсы — а для планировщика она невидимка. Я назвал это *ghost setup*.

<spoiler title="Техническое исправление">
Каждая переналадка теперь создаёт опциональный интервал в CP-SAT, и этот интервал входит в cumulative-ограничения наравне с обработкой. Greedy резервирует setup + processing атомарно.
</spoiler>

Баг прошёл все юнит-тесты. Ни в одном тесте не было инстанса, где переналадка и ARC пересекались бы. Hypothesis поймал бы. Но я добавил Hypothesis *после* этого бага. Классика.

## FeasibilityChecker: 7 проверок после каждого solve()

Любой решатель может врать. CP-SAT — из-за бага в модели. Greedy — из-за ошибки в диспетчеризации. LBBD — из-за неконсистентности при сборке подзадач.

Семь проверок на готовом расписании:

1. **Полнота** — каждая операция назначена
2. **Допустимость** — станок из `eligible_wc_ids`
3. **Прецеденты** — операция 2 не раньше конца операции 1
4. **Ёмкость** — нет перекрытий (обработка + setup)
5. **Setup gaps** — зазор ≥ SDST-матрица
6. **ARC** — $\sum q_{ir} \leq C_r$ в каждый момент $t$, **включая setup**
7. **Горизонт** — ни одна операция за planning horizon

Это **runtime-инвариант**, не тест в CI. На каждом вызове, для каждого решателя.

### Защита от бага в самом валидаторе

1. Cross-solver consistency: CP-SAT и Greedy на одном инстансе через один FeasibilityChecker
2. Hypothesis: случайные невалидные расписания — checker обязан поймать
3. Ловушки: 7 типов нарушений × 7 сконструированных расписаний

## Железо и перформанс

Профиль medium-stress (500 ops, плотная SDST):

| Фаза | Время | Где |
|------|-------|-----|
| JSON → Pydantic parse | 12 мс | Python |
| Model building (CP-SAT) | 45 мс | Python → protobuf |
| **CP-SAT solve** | **28 400 мс** | **C++ ядро OR-Tools** |
| Result extraction | 8 мс | Python |
| FeasibilityChecker | 3 мс | Python |

Python-код: 68 мс из 28 468 мс (**0.24%**). Переписать Python на Rust — сэкономить 68 мс из 28 секунд. Узкое место — всегда C++ солвер.

CP-SAT использует portfolio search — 8 стратегий параллельно. EPYC 7763 (64 cores): gap < 3% за 30 с на 300 ops. i7-10700 (8 cores): gap 12% за 30 с.

### Чего нет и почему

- **NumPy** — используется в `SdstMatrix` для O(1) SDST-lookup в ALNS/RHC. Основная модель — Pydantic, не NumPy
- **pandas** — JSON → Pydantic → JSON. pandas не нужен
- **TensorFlow/PyTorch** — нет ML в ядре. Когда будет GNN — добавится
- **Docker** — в roadmap

## Любой завод, не только кабельный

SynAPS решает абстрактную задачу MO-FJSP-SDST-ARC. Ему нужны: операции (id, длительность, дедлайн, допустимые станки), рабочие центры, SDST-матрица, ARC, прецеденты. JSON на входе, JSON на выходе. Одна Pydantic-схема, 347 строк.

## ε-Constraint: три расписания вместо одного

Скаляризация — жульничество: makespan доминирует. ε-constraint (Mavrotas, 2009) даёт реальный выбор:

| Профиль | Что делает |
|---------|-----------|
| `CPSAT-EPS-SETUP-110` | Зажать makespan ≤ 110%, давить переналадки |
| `CPSAT-EPS-TARD-110` | Зажать makespan ≤ 110%, давить просрочки |
| `CPSAT-EPS-MATERIAL-110` | Зажать makespan ≤ 110%, давить потери материала |

Плановик получает три расписания с числами. Не фронт Парето из 1000 точек.

## Ремонт на лету: станок сдох

`IncrementalRepair` замораживает всё за пределами окрестности сбоя. Радиус — от типа аварии: поломка → 2 зависимых перенладки вниз; срочный заказ → ±30 мин; нехватка материала → операции в той же state-группе. Внутри — greedy re-dispatch, затем micro-CP-SAT. Цель — нервозность < 5%.

## Квантовые вычисления: честно

QUBO-кодирование scheduling:

$$H = A \sum_i \Bigl(1 - \sum_j x_{ij}\Bigr)^2 + B \sum_j \text{penalty}(j) + C \cdot \text{objective}$$

D-Wave Advantage: 5 000+ кубитов хватает на ~50 операций. 50K операций — ~10M физических кубитов. Горизонт 2035–2040.

Архитектура SynAPS не привязана к бэкенду. Роутер добавит ветку `QAOA-HYBRID` когда физики разберутся. Но я не рисую слайд «quantum-ready» без пометки.

## Что реально работает, а что нет

**Реализовано и протестировано:**
- 12 решателей, 7 419 строк solver-кода, 250 тестов
- 21 конфигурация в реестре
- Log-ATCS без float64 underflow
- Ghost setup fix
- Beam Search для тяжёлых матриц
- LBBD greedy warm start + параллельные подзадачи
- ALNS: 4 destroy-оператора, micro-CP-SAT repair, SA acceptance
- RHC: скользящее окно для 10K–100K+ операций
- SdstMatrix: NumPy O(1) SDST-lookup
- Генератор инстансов (19 пресетов, до 50K+)
- JSON Schema контракты из Pydantic

**Не реализовано (roadmap):**
- Промышленная интеграция — **код не тестировался на живом заводе**
- Rust через PyO3 — заглушка, нативный модуль не собирается
- Иерархическая LBBD для >10K операций (без RHC)
- GNN для отсечений Бендерса
- LLM-пояснения для оператора
- Квантовый бэкенд (~2035)
- Docker, GUI, MES-интеграция

---

### Литература

1. Pinedo M.L. (2016). *Scheduling: Theory, Algorithms, and Systems*, 4th ed. Springer.
2. Allahverdi A. et al. (2008). A survey of scheduling problems with setup times or costs. *EJOR*, 187(3), 985–1032.
3. Lee Y.H., Bhaskaran K., Pinedo M. (1997). A heuristic to minimize the total weighted tardiness with sequence-dependent setups. *IIE Trans.*, 29(1), 45–52.
4. Hooker J.N., Ottosson G. (2003). Logic-Based Benders Decomposition. *Math. Prog.*, 96, 33–60.
5. Naderi B., Roshanaei V. (2022). Critical-path-search LBBD for FJSP. *INFORMS J. on Optimization*.
6. Mavrotas G. (2009). Effective implementation of the ε-constraint method. *Appl. Math. Comput.*, 213(2), 455–465.
7. Ow P.S., Morton T.E. (1989). The single machine early/tardy problem. *Management Science*, 35(2), 177–191.
8. Shingo S. (1985). *A Revolution in Manufacturing: The SMED System*. Productivity Press.
9. Farhi E. et al. (2014). A Quantum Approximate Optimization Algorithm. arXiv:1411.4028.
10. Venturelli D. et al. (2016). Quantum Annealing Implementation of Job-Shop Scheduling. arXiv:1506.08479.
11. Shaw P. (1998). Using Constraint Programming and Local Search Methods to Solve Vehicle Routing Problems. *CP-98*.
12. Ropke S., Pisinger D. (2006). An Adaptive Large Neighborhood Search Heuristic for PDPTW. *Transportation Science*, 40(4), 455–472.
13. Rawlings J.B., Mayne D.Q. (2009). *Model Predictive Control: Theory and Design*. Nob Hill Publishing.

---

Код: [github.com/KonkovDV/SynAPS](https://github.com/KonkovDV/SynAPS). MIT лицензия. `pip install -e ".[dev]"` и поехали.
